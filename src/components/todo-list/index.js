import λ from 'react-on-lambda'
import styled from 'styled-components'

const list = styled.div`
  background-color: white;
  border: 1px solid #e2e2e2;
  margin-top: 25px;
  padding: 24px;
`

export default λ(list)
